@model IEnumerable<BloodBankManagementSystem.Models.BloodStock>

@{
    ViewBag.Title = "BloodStock";
}

<h2>BloodStock</h2>

<p>
    @Html.ActionLink("Create New", "Createbloodstock")
</p>
<table class="table table-bordered alert-info">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.bloodgroup)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.bloodcomponents)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.quantity)
        </th>




        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.bloodgroup)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.bloodcomponents)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.quantity)
    </td>




    @*<td>
        @Html.ActionLink("Edit", "Edit", new { id = item.bloodstockid }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.bloodstockid })
    </td>*@
</tr>
}

</table>
